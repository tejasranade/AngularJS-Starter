import template from './login.html';
import controller from './login.controller';

let booksComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller,
  controllerAs: 'vm'
};

export default booksComponent;
